import React from 'react'

const Button = ({name}) => {
  return (
    <div>
      <button className='m-2 p-2 bg-[#F2F2F2] rounded-md font-bold'>{name}</button>
    </div>
  )
}

export default Button
